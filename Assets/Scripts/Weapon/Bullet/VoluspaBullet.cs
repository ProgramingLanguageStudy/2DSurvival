using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 볼루스파 총알을 구현할 클래스
/// ProjectileBullet의 일자로 가는 능력자체를 상속받는다.
/// </summary>
public class VoluspaBullet : ProjectileBullet
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
